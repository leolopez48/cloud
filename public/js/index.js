const back = document.getElementById('back').addEventListener('click', (ev) => {
    ev.preventDefault();

    alert('Click');
});

const back = document.getElementById('folder').addEventListener('click', (ev) => {
    ev.preventDefault();

    alert('Click');
});